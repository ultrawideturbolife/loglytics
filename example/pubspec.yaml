name: loglytics_example
description: Loglytics example project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  loglytics:
    git:
      url: https://github.com/codaveto/loglytics.git
      ref: main

  firebase_crashlytics: ^2.2.4
  firebase_analytics: ^8.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
